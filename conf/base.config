/*
 * base.config — shared process resource defaults.
 * Included by profiles that don't override.
 */

process {
    errorStrategy = { task.exitStatus in [143, 137, 104, 134, 139] ? 'retry' : 'finish' }
    maxRetries    = 2
    maxErrors     = '-1'
}
